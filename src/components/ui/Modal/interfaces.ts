export declare namespace ModalInterface {
  export namespace Style {
    export interface Modal {
      zIndex: number;
    }

    export interface Overlay {
      zIndex: number;
    }

    export interface Frame {
      width: string;
      maxWidth: string;
      maxHeight: string;
      zIndex: number;
    }
  }
}
