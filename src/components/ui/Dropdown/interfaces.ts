export declare namespace DropdownInterface {
  export interface Class {
    relative: boolean;
  }

  export interface Style {
    minWidth: string;
    minHeight: string;
    maxWidth: string;
    maxHeight: string;
  }
}
