export declare namespace FormSelectInterface {
  export interface Class {
    disabled: boolean;
  }

  export interface Props {
    icon: string;
    items: string[];
    placeholder: string;
    disabled: boolean;
  }
}
